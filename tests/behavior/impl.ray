// EXIT_CODE: 1

type T = { inner: i32 }

impl T {
    fn new(inner: i32) -> T {
        T { inner: inner }
    }
}

impl T {
    fn new2() -> T {
        T { inner: 420 }
    }
}

fn main() -> bool {
    let t1 = T::new(69);
    let t2 = T::new2();
    t1.inner == 69 && t2.inner == 420
}

