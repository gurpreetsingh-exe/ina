extern mod core;

type Test[T] = {
    inner: T
}

#[test]
fn with_type_param() {
    let a = Test![i32] { inner: 1 };
    let b: i32 = 1;
    assert a.inner == b;
    let b = Test![str] { inner: "Hello World" };
    assert (&(b.inner)).len() == 11;
}
