// EXIT_CODE: 0

type Test[T] = {
    inner: T
}

extern "intrinsic" {
    fn len(str: &str) -> usize;
}

fn main() -> i32 {
    let a = Test![i32] { inner: 1 };
    let b: i32 = 1;
    assert a.inner == b;
    let b = Test![str] { inner: "Hello World" };
    assert len(&(b.inner)) == 11;
    0
}
