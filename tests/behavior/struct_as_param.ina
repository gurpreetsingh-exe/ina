// EXIT_CODE: 1

type Foo = {
    a: &i32,
    b: i32
}

fn some(foo: Foo) -> i32 {
    *(foo.a) + foo.b
}

fn main() -> bool {
    let a = 34;
    let foo = Foo { a: &a, b: 35 };
    some(foo) == 69
}
