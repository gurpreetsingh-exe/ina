extern mod core;
using core::prelude::*;

fn test(v: Option[usize]) -> bool {
    match v {
        Some(0 | _) => true,
        None => false,
    }
}

[test]
fn wild() {
    assert test(Some(42));
    assert test(None) == false;
}
