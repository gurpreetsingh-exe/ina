type Foo =
    | A(i32)
    | B(i32)

fn main() {
    let x = match Foo::A(20) {
        Foo::A(x) | Foo::B(x) => x
    };
    assert x == 20;
    let Foo::A(x) | Foo::B(x) = Foo::B(50);
    assert x == 50;
}
