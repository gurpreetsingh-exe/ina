fn test(a: i32, b: i32) -> i32 {
    let c = {
        let a = 50;
        a + b
    };
    c
}

[test]
fn ret() {
    let a = { 50 };
    assert a == 50;
    assert { 1 + 1 } == { 2 };
    assert { test(2, 4) } == 54;
}
