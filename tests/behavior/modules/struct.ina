mod foo {
    type Foo = {
        a: &i32,
        b: i32
    }
}

type Foo = {
    a: foo::Foo,
}

[test]
fn struct_import() {
    let a = 34;
    let foo = foo::Foo { a: &a, b: 35 };
    let bar = Foo { a: foo };
    assert *(bar.a.a) + bar.a.b == 69;
}
