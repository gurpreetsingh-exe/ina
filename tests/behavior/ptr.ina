extern mod core;

#[test]
fn check_str_deref() {
    let str = "a";
    assert *(str.as_ptr()) == 97;
}

#[test]
fn read_write() {
    let mut a = 20;
    let b = &mut a as *mut i32;
    assert *b == 20;
    *b = 58;
    assert *b == 58;
}
