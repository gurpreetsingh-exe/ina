type Foo = {
    a: &i32,
    b: i32
}

type Bar = {
    a: Foo,
}

#[test]
fn struct_ref_in_struct() {
    let a = 34;
    let foo = Foo { a: &a, b: 35 };
    let bar = Bar { a: foo };
    assert *(bar.a.a) + bar.a.b == 69;
}
