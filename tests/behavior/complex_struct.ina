// EXIT_CODE: 1

type Foo = {
    a: &i32,
    b: i32
}

type Bar = {
    a: Foo,
}

fn main() -> bool {
    let a = 34;
    let foo = Foo { a: &a, b: 35 };
    let bar = Bar { a: foo };
    *(bar.a.a) + bar.a.b == 69
}
