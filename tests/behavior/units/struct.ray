// DUMMY

type Unit = {}

impl Unit {
    fn new() -> Unit {
        Unit {}
    }
}

// fails with assoc fn name "f1"
impl Unit {
    fn f2(self) { f1(self, 0); }
}

fn f1(_: Unit, _: i32) {}

mod StrBuf {
    type t = {
        ptr: *i8,
        len: usize,
    }

    fn new() -> t {
        t { ptr: 0 as *i8, len: 0 }
    }

    fn len(t: t) -> usize { t.len }
}

