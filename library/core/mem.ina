using mod::intrinsics;

fn sizeof[T]() -> usize { intrinsics::sizeof![T]() }
fn transmute[Src, Dst](src: Src) -> Dst { intrinsics::transmute(src) }
fn discriminant[T](v: T) -> usize { intrinsics::discriminant(v) }
