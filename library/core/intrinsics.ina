extern "intrinsic" {
    fn len[T](str: [T]) -> usize;
    fn offset[T](ptr: *T, off: usize) -> *T;
    fn transmute[Src, Dst](src: Src) -> Dst;
    fn discriminant[T](v: T) -> usize;
    fn sizeof[T]() -> usize;
}
