using os::linux::syscall;

type FileDesc = {
    fd: i32
}

impl FileDesc {
    fn from_fd(fd: i32) -> FileDesc {
        FileDesc { fd: fd }
    }

    fn write(&self, bytes: [u8]) {
        syscall::write(self.fd, bytes.as_ptr(), bytes.len());
    }
}
